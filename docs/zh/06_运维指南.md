[English](../en/06_运维指南.md) | 简体中文

# 运维指南

## Prometheus 指标

### 启用指标

```yaml
metrics:
  enabled: true
```

指标端点：`http://{host}:{metrics_port}/metrics`，默认 `http://127.0.0.1:8002/metrics`。

### 内置指标

| 指标名 | 类型 | 说明 |
|--------|------|------|
| `litserve_request_duration_seconds` | Histogram | 请求处理延迟 |
| `litserve_requests_total` | Counter | 总请求数 |
| `litserve_active_requests` | Gauge | 活跃请求数 |

### 自定义指标

在模型中定义：

```python
from prometheus_client import Counter, Histogram

class MyAPI(LitAPI):
    input_tokens = Counter("my_input_tokens_total", "Input tokens")
    predict_latency = Histogram("my_predict_seconds", "Predict latency")

    def predict(self, x, **kwargs):
        self.input_tokens.inc(len(x))
        with self.predict_latency.time():
            return self.model(x)
```

自定义指标自动通过 multiproc 模式聚合。

---

## 日志

### 日志模式

| 模式 | 说明 | 适用场景 |
|------|------|----------|
| `queue` | 异步队列写入 | 高并发，默认 |
| `file` | 直接文件写入 | 简单场景 |

### 日志配置

```yaml
logging:
  mode: "queue"
  level: "info"
  format: "json"
  info_output: "./logs/info.log"
  error_output: "./logs/error.log"
  rotate_by: "size"
  max_size: 100
  backup_count: 7
```

### 格式

**JSON 格式（默认）：**

```json
{"timestamp":"2024-01-01T12:00:00","level":"INFO","message":"Model loaded","model":"test_model"}
```

**Text 格式：**

```
2024-01-01 12:00:00 [INFO] Model loaded: test_model
```

### 在模型中使用日志

```python
def predict(self, x, **kwargs):
    self.logger.info(f"predict called with input={x}")
    self.logger.warning("Something unexpected")
    return self.model(x)
```

---

## 制品打包（Artifact）

### 打包模型

```bash
light-server pack model_repo/test_model --version 1.0.0
```

输出：`artifacts/test_model-1.0.0-{build_id}.lma`

`.lma` 文件是一个 ZIP 压缩包，包含：

- `manifest.json`：元数据（名称、版本、构建 ID、文件列表、哈希）
- 模型文件

> **运行时加载：** 服务器启动时会自动扫描模型仓库中的 `.lma` 文件（通过 `_scan_artifact_models`）。你可以直接将 `.lma` 制品放入 `model_repo/` 而无需手动解压 — 服务器会自动提取到缓存并按普通模型加载。

### 签名（可选）

```bash
light-server pack model_repo/test_model --version 1.0.0 --sign-key private.pem --signer "ci@company.com"
```

### 解包

```bash
light-server unpack artifact.lma --to ./model_repo
```

### 校验

```bash
light-server unpack artifact.lma --verify-key public.pem --dry-run
```

---

## 模型分析器

分析器自动搜索最优配置组合：

```bash
light-server analyze --model test_model --output-dir ./reports
```

搜索空间：

- `max_batch_size`：1, 2, 4, 8, 16
- `batch_timeout`：0.001, 0.01, 0.1
- `concurrency`：1, 2, 4, 8, 16

报告输出：

```json
{
  "model": "test_model",
  "pareto_frontier": [
    {
      "max_batch_size": 4,
      "batch_timeout": 0.01,
      "concurrency": 8,
      "throughput": 520.5,
      "latency_p99": 25.3
    }
  ]
}
```

Pareto 前沿上的配置是在吞吐量和延迟之间取得最佳平衡的点。

---

## Web UI

### 启用

```yaml
webui:
  enabled: true
```

### 访问

打开浏览器访问 `http://{host}:{http_port}/`，默认 `http://127.0.0.1:8000/`。

### 功能与操作

**模型管理**
- 首页展示所有已加载模型的名称、版本、就绪状态
- 点击模型卡片查看详细信息（API 路径、批处理配置、加速器）
- 通过"加载" / "卸载"按钮动态管理模型，无需重启服务

**推理测试**
- 选择模型后，在测试面板输入 JSON payload
- 点击"发送请求"，实时查看响应结果和请求耗时
- 支持复制 curl 命令，方便在终端复现

**指标监控**
- 自动拉取 `:metrics_port/metrics` 的 Prometheus 数据
- 以图表形式展示请求延迟分布、吞吐量趋势、活跃请求数
- 按模型维度筛选，快速定位性能瓶颈

**分析器报告**
- 若运行过 `light-server analyze`，报告会自动展示在 WebUI
- 查看 Pareto 前沿配置列表，一键应用推荐参数

### 安全配置

生产环境中，建议将 WebUI 与管理 API 放在同一端口，通过反向代理（如 Nginx）增加认证：

```nginx
location / {
    auth_basic "Light Server Admin";
    auth_basic_user_file /etc/nginx/.htpasswd;
    proxy_pass http://127.0.0.1:8000;
}
```

---

## 下一步

- [架构设计](07_架构设计.md)
- [FAQ](08_常见问题.md)
