server:
  host: 127.0.0.1
  http_port: 8000
  log_level: info

grpc:
  enabled: false

metrics:
  enabled: false

model_repository:
  path: ./model_repo
  control_mode: explicit

load_models:
  - llm_model
